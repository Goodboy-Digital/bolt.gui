import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks';

import { BoltContainer } from '../new/BoltComponent';
import { WindowContainer } from '../panels/WindowContainer';
import { Label } from '../inputs';

<Meta title="Components/Bolt" component={BoltContainer} />

# Bolt

This is `Bolt GUI`

```jsx
import { BoltContainer } from 'my-components'
```

<Preview withSource="open">
  <Story name="Bolt gGUI">
    <BoltContainer viewData={[
    {
        component: WindowContainer,
        props: {
            defaultContainerPosition: { x: '50px' , y: '50px' },
            expandedX: true,
            expandedY: true,
            expandYCallBack: () => {console.log('hi')},
            setActiveTabCallBack: () => {},
            panelData: [
            {
                title: 'Example',
                id: 'E',
                elements: [
                    {
                        rows: [
                            {
                                components: [
                                    { component: Label, inputData: { label: 'Hello world! Welcome to Bolt.gui', textAlign: 'center' }},
                                ]
                            },
                        ]
                    }
                ],
            },
            ],
            activeTabIndex: 0
        }
      },
    ]}
></BoltContainer>
  </Story>
</Preview>
